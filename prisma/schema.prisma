generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("MONGODB_URI")
}

model account {
  id           String @id @default(auto()) @map("_id") @db.ObjectId
  currentValue Float
  initialValue Float
  name         String
}
